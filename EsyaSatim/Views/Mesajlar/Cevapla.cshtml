@{
    ViewBag.Title = "MesajGonder";
    Layout = "~/Views/Shared/_KullaniciLayout.cshtml";
}
@model EsyaSatim.ViewModels.UrunlerIndex
<br />
<br />
<br />
<br />
<div class="seipkon-login-form-area">
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-4 col-md-offset-4">
                <div class="login-form-box" style="box-sizing:border-box">

                    <form method="post" action="/Mesajlar/MesajGonder">
                        @using (Html.BeginForm())
                        {
                            @Html.ValidationSummary()

                            <div class="form-group">
                                <p>Mesajınız</p>
                            </div>
                            <input type="hidden" id="Alici_id" name="Alici_id" value="@ViewData["id"]" />
                            <div class="form-group">
                                <textarea name="Mesaj" class="the-area"></textarea>
                            </div>
                            <p>
                                <button type="submit" class="btn btn-success"> <i class="fa fa-envelope-o"></i>Gönder</button>
                            </p>

                            @*<div class="form-group">
                                    <p>Mesajınız</p>
                                </div>
                                <input type="hidden" id="Alici_id" name="Alici_id" />
                                <div class="form-group">
                                    <textarea name="Mesaj" class="the-area"></textarea>
                                </div>
                                <div class="form-group">
                                    <div class="row">
                                        <div class="col-md-12">
                                            <div class="form-layout-submit">
                                                <input type="submit" value="Send" class="button-4">
                                            </div>
                                        </div>
                                    </div>
                                </div>*@
                        }

                    </form>

                </div>
            </div>
        </div>
    </div>
</div>